{
  "name": "swagger-express-middleware-wrapper",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@types/node": "14.0.4",
    "eslint": "^8.32.0",
    "prettier": "^2.8.3",
    "rimraf": "~4.1.0",
    "typescript": "^4.9.4"
  },
  "scripts": {
    "build:cjs": "tsc --module commonjs",
    "build:esm": "tsc --module esnext",
    "build": "yarn run build:cjs && yarn run build:esm",
    "clean": "rimraf dist",
    "develop": "yarn serve | pino-pretty",
    "serve": "cd dist && node ./index.js"
  },
  "dependencies": {
    "pino-pretty": "~9.1.1"
  }
}
